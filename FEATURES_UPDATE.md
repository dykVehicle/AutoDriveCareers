# 功能更新说明

## 新增功能

### 1. 用户数据服务器端持久化

**功能描述**：
- 支持将候选人和企业账户数据保存到服务器端，而不是仅存储在浏览器的 localStorage 中
- 用户数据在服务器端持久化后，可以在不同设备、不同浏览器间同步
- 支持本地存储和服务器存储两种模式，可通过环境变量灵活切换

**实现方式**：
1. 创建了 `src/services/api.ts` - 封装所有后端API调用
2. 修改了 `src/services/auth.ts` - 支持服务器端和本地存储双模式
3. 通过环境变量 `VITE_API_BASE_URL` 控制是否启用服务器端存储

**配置方法**：
```bash
# 1. 复制环境变量模板
cp .env.example .env

# 2. 编辑 .env 文件，配置后端API地址
# VITE_API_BASE_URL=http://localhost:3000/api

# 3. 如果不配置此变量，将使用本地 localStorage 存储
```

**优势**：
- ✅ 数据安全：用户数据存储在服务器，不会因清除浏览器数据而丢失
- ✅ 多端同步：用户可以在不同设备上登录，访问相同的账户数据
- ✅ 灵活切换：开发阶段可使用本地存储，生产环境切换到服务器存储
- ✅ 向下兼容：未配置API时，应用仍可正常使用本地存储

### 2. 简历导出功能

**功能描述**：
- 支持将生成的简历导出为 **PDF** 格式
- 支持将生成的简历导出为 **Word文档** (DOCX) 格式
- 导出的文件自动命名为：`{姓名}_{日期}.pdf` 或 `{姓名}_{日期}.docx`

**实现方式**：
1. 安装了必要的导出库：
   - `jspdf` - PDF生成
   - `docx` - Word文档生成
   - `file-saver` - 文件保存

2. 创建了 `src/services/export.ts` - 封装导出逻辑
   - `exportResumeToPDF()` - 导出PDF功能
   - `exportResumeToWord()` - 导出Word功能

3. 更新了 `src/views/ResumeBuilder.vue` - 添加导出按钮
   - 📄 导出 PDF 按钮
   - 📝 导出 Word 按钮

**使用方法**：
1. 在简历生成器中粘贴或填入简历内容
2. 点击"生成预览"按钮
3. 点击"📄 导出 PDF"或"📝 导出 Word"按钮
4. 文件会自动下载到浏览器默认下载目录

**支持的简历内容**：
- ✅ 个人基本信息（姓名、职位、城市、联系方式）
- ✅ 个人简介
- ✅ 技能列表
- ✅ 工作经历
- ✅ 项目经历
- ✅ 教育经历
- ✅ 证书
- ✅ 奖项

**导出格式特点**：
- **PDF格式**：
  - 使用 A4 纸张大小
  - 清晰的排版和间距
  - 支持自动分页
  - 适合打印和在线展示

- **Word格式**：
  - 标准 DOCX 格式
  - 可二次编辑
  - 支持标题层级
  - 保留完整格式

---

## 技术变更

### 新增依赖包

```json
{
  "dependencies": {
    "axios": "^1.13.2",         // HTTP客户端
    "jspdf": "^2.x.x",          // PDF生成
    "docx": "^8.x.x",           // Word文档生成
    "file-saver": "^2.x.x"      // 文件保存
  }
}
```

### 新增文件

1. `src/services/api.ts` - 后端API服务配置
2. `src/services/export.ts` - 简历导出服务
3. `.env.example` - 环境变量配置模板
4. `BACKEND_API.md` - 后端API接口文档
5. `FEATURES_UPDATE.md` - 本功能更新说明

### 修改文件

1. `src/services/auth.ts` - 支持服务器端和本地存储双模式
2. `src/stores/auth.ts` - 更新为异步的邮箱验证码发送
3. `src/views/ResumeBuilder.vue` - 添加PDF和Word导出按钮
4. `package.json` - 添加新的依赖包

---

## 后端API实现指南

如果您希望使用服务器端持久化功能，需要实现相应的后端API。

详细的API接口规范和实现建议请参考：[BACKEND_API.md](./BACKEND_API.md)

**必需的API接口**：
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/send-code` - 发送邮箱验证码
- `GET /api/auth/me` - 获取当前用户信息
- `POST /api/auth/logout` - 用户登出

**推荐技术栈**：
- Node.js + Express / Fastify
- PostgreSQL / MySQL / MongoDB
- JWT 认证
- bcrypt 密码加密

---

## 使用示例

### 场景1：开发环境（使用本地存储）

```bash
# 不配置 .env 文件，或设置：
# VITE_API_BASE_URL=disabled

npm run dev
# 用户数据会存储在浏览器 localStorage 中
```

### 场景2：生产环境（使用服务器存储）

```bash
# 1. 配置 .env 文件
echo "VITE_API_BASE_URL=https://api.yourdomain.com/api" > .env

# 2. 构建应用
npm run build

# 3. 部署
# 所有用户注册和登录都会调用服务器API
# 数据持久化在服务器数据库中
```

### 场景3：导出简历

```bash
# 1. 启动应用
npm run dev

# 2. 访问 http://localhost:5173
# 3. 注册/登录为候选人
# 4. 进入"简历生成器"页面
# 5. 填入或粘贴简历内容
# 6. 点击"生成预览"
# 7. 点击"📄 导出 PDF"或"📝 导出 Word"
# 8. 文件自动下载
```

---

## 注意事项

1. **环境变量配置**：
   - `.env` 文件不应提交到版本控制（已在 `.gitignore` 中）
   - 生产环境需要单独配置环境变量

2. **API兼容性**：
   - 前端支持本地存储和服务器存储自动切换
   - 即使没有后端API，应用也能完全正常工作

3. **简历导出质量**：
   - PDF和Word导出使用客户端生成，无需后端支持
   - 导出的简历支持中文显示
   - 建议在生成预览后再导出，确保内容正确

4. **数据迁移**：
   - 从本地存储切换到服务器存储时，旧数据不会自动迁移
   - 用户需要重新注册或手动导入数据（可后续开发迁移工具）

---

## 未来优化建议

1. **简历导出增强**：
   - 支持更多简历模板样式
   - 支持自定义配色方案
   - 添加简历预览后的编辑功能

2. **数据同步**：
   - 实现本地存储到服务器的自动迁移工具
   - 支持简历草稿自动保存到服务器
   - 支持简历版本历史管理

3. **用户体验**：
   - 添加导出进度提示
   - 支持批量导出多份简历
   - 支持简历模板市场

4. **安全性**：
   - 实现双因素认证
   - 添加账号安全中心
   - 支持数据加密存储

---

## 问题反馈

如有任何问题或建议，请通过以下方式反馈：
- GitHub Issues
- 邮件联系开发团队
